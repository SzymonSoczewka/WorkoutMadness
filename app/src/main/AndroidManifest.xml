<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team12.workoutmadness">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GUI.activities.MainActivity"
            android:windowSoftInputMode="stateHidden|adjustResize|adjustPan"
            android:screenOrientation="nosensor">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GUI.activities.LoginActivity">

        </activity>
        <activity android:name=".GUI.activities.DayActivity">

        </activity>
        <activity android:name=".GUI.activities.ExerciseActivity">

        </activity>
        <activity android:name=".GUI.activities.CreateAccountActivity">

        </activity>
    </application>

</manifest>